# CHANGELOG

## v1.1.0 — 2026-05-10

### 新增
- `/role add <序号|身份名> [广播0/1]`：支持在加入身份组时直接指定广播接收意愿，省去交互询问步骤
- `/role adminadd <QQ> <序号> [广播0/1]`：管理员添加用户时同样支持直接指定广播选择
- 重复加入身份组时不再报错，而是**更新**该用户的广播接收状态
- `/role show [QQ号]`：查看自己或指定用户所加入的所有身份组及广播状态
- `/role add` 支持通过**身份组名称**直接加入（原仅支持序号）
- `/role at <身份名> [附加消息]`：AT 时可附带自定义消息内容
- 身份组名称创建时禁止使用纯数字，防止与序号混淆

### 变更
- 数据文件路径从 `data/role_at_data.json` 迁移至 `data/plugin_data/astrbot_plugin_role_at/role_at_data.json`

---

## v1.0.0 — 2026-05-09

### 新增
- `/role addpreset <名称> <上限> <广播0/1> <可见0/1>`：创建预设身份组
- `/role delpreset <名称>`：删除预设身份组
- `/role list`：查看本群所有可见身份组
- `/role add <序号>`：加入身份组
- `/role remove <序号>`：退出身份组
- `/role adminadd <QQ> <序号>`：管理员为指定用户添加身份
- `/role adminremove <QQ> <序号>`：管理员移除指定用户的身份
- `/role at <身份名>`：AT 指定身份组中接受广播的成员
- `/role freeat <0/1>`：控制普通用户是否可使用 AT 功能
- 加入广播身份组时自动询问用户是否接收广播，支持文字回复确认
- 广播确认待响应超时自动清理（5 分钟）
- 数据持久化，重启不丢失；各群数据相互隔离
